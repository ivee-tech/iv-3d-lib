import * as THREE from 'three';
import { WglUtil } from './wgl-util';
import { SimpleDictionary } from './models/simple-dictionary';
import { Timeline } from './models/timeline';
import { ITimelinePlugin } from './i-timeline-plugin';
import { ErrorService } from './services/error-service';
import { DataModel } from './models/data-model';
export declare class ThreeJsTerrainPlugin implements ITimelinePlugin {
    private _errorSvc;
    config: SimpleDictionary<string>;
    timeline: Timeline;
    data: DataModel;
    w: WglUtil;
    mainGroup: THREE.Group;
    private heightMap;
    private normalMap;
    private uniformsNoise;
    private uniformsNormal;
    private terrain;
    private uniformsTerrain;
    private SCREEN_WIDTH;
    private SCREEN_HEIGHT;
    private quadTarget;
    private mlib;
    private sceneRenderTarget;
    private lightVal;
    private lightDir;
    private updateNoise;
    private animDelta;
    private animDeltaDir;
    private cameraOrtho;
    private directionalLight;
    private pointLight;
    private clock;
    constructor(_errorSvc: ErrorService);
    init(): void;
    load(): void;
    update(time?: number): void;
    private addTerrain();
    private updateTerrain();
}
